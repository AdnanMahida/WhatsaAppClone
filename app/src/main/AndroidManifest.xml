<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsAppClone">
        <activity android:name=".ui.chat_details.ChatInfoActivity" />
        <activity android:name=".ui.chat_details.ChatDetailsActivity" />
        <activity android:name=".ui.authentication.SignInActivity" />
        <activity android:name=".ui.authentication.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.authentication.SignUpActivity" />
        <activity android:name=".ui.home.HomeActivity" />
    </application>

</manifest>